'use strict';

export function countLetters(myString: string) {
  let stringArray: [] = myString.split('').sort();
  let map = {};
  for (let i: number = 0; i < myString.length; i++)
    map[]

  
  
  return
  
}